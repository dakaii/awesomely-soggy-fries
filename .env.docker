# Docker Development Environment
NODE_ENV=development

# Database Configuration (runs in Docker network)
DB_HOST=db
DB_PORT=5432
DB_NAME=chirp_db

# Application Configuration
PORT=3000
DOCKER_ENV=true

# JWT Secret (change in production)
JWT_SECRET=docker-dev-jwt-secret-key
